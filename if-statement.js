let fruit = "orange";
let length = fruit.length;

if (length > 5) {
	fruit = "The fruit name has more than five characters."
}
	else if (length < 5) {
	fruit = "The fruit name has five characters or less."
}

console.log(fruit);